<!-- markdownlint-disable-file -->
# Release Changes: Epic3 PR Feedback Remediation

**Related Plan**: .copilot-tracking/plans/2026-06-30/epic3-pr-feedback-remediation-plan.instructions.md
**Implementation Date**: 2026-06-30

## Summary

Implement targeted server-side fixes and tests for replay-window runtime wiring, realtime fanout registry/dispatch wiring, and outbound cap reset recovery behavior.

## Changes

### Added

* apps/server/tests/unit/startup-bootstrap.test.ts - Added bootstrap wiring test that asserts replay window runtime config is passed to tile repository construction.

### Modified

* apps/server/src/index.ts - Wired `placementCommandReplayWindowSeconds` into tile repository options as `replayWindowSeconds`.
* apps/server/src/http/app.ts - Replaced no-op publish path with registry entry contract carrying subscriber resolution and sender callback; added shared registry key helper.
* apps/server/src/rooms/arena.room.ts - Registered room coordinator into shared registry on create and cleaned registry/coordinator resources on dispose.
* apps/server/src/domain/delta-fanout.service.ts - Added outbound cap recovery by resetting subscriber outbound counters when reset window elapses.
* apps/server/artifacts/e3-s4-latency-budget.json - Updated load artifact output generated by workspace test run.
* apps/server/tests/unit/room-auth.test.ts - Added lifecycle test for fanout registry register and cleanup behavior.
* apps/server/tests/unit/delta-fanout.service.test.ts - Added outbound cap reset-window recovery and pre-reset block assertions.
* apps/server/tests/integration/realtime-delta-fanout.integration.test.ts - Added placement dispatch integration assertion and cap stability/recovery coverage.

### Removed

* None.

## Additional or Deviating Changes

* Outbound cap reset-window implementation uses existing `deltaAckPendingTtlMs` configuration as reset interval.
	* Reason: Chosen to keep runtime configuration surface stable and avoid introducing a separate outbound reset setting in this remediation PR.
* Workspace was already dirty with planning/research artifacts prior to implementation.
	* Reason: Pre-existing `.copilot-tracking` files were left intact and not reverted.

## Release Summary

All four planned remediation findings were implemented and validated.

Files affected:

* Added: 1 file
* Modified: 8 files
* Removed: 0 files

Validation status:

* Server-focused validation passed:
	* `npm run -w @game/server lint`
	* `npm run -w @game/server build`
	* `npm run -w @game/server test -- tests/unit/startup-bootstrap.test.ts`
	* `npm run -w @game/server test -- tests/unit/room-auth.test.ts`
	* `npm run -w @game/server test -- tests/unit/delta-fanout.service.test.ts`
	* `npm run -w @game/server test -- tests/integration/realtime-delta-fanout.integration.test.ts`
* Full workspace validation passed:
	* `npm run build`
	* `npm run lint`
	* `npm run test`

Deployment/infrastructure notes:

* No deployment script or infrastructure changes were made.
* Follow-on work remains tracked in planning log for CI latency gate and additional fanout contract hardening tests.
